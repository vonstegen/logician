Deterministic rule enforcement: provable policies, audit trails, cost checks

Creating a separate rule follower or checker for the LLM to prevent hallucinations when working with the LLM.

---
[ESCAPING THE CAGED PROCESSOR: ARCHITECTING THE LOGICIAN](https://augmentedmind.substack.com/p/escaping-the-caged-processor)
https://substack.com/inbox/post/190633750

Why your AI constantly forgets your rules, and how a deterministic “Symbiotic Shield” permanently fixes the hallucination trap.

Why your AI constantly forgets your rules, and how a deterministic “Symbiotic Shield” permanently fixes the hallucination trap.
https://youtu.be/v8_PHdYM_L0?si=8Y7ev0AKRWF7DxWU


You are failing because you are treating a probabilistic engine like a deterministic machine. The 42% failure rate you experience is not a software bug. It is the default state of the Billion-Parameter Trap.

Think about the actual cost. Consider the lost hours spent re-explaining your brand voice, the cognitive fatigue of acting as a permanent auditor for an amnesiac AI, and the creeping dread when your outputs revert to the generic, corporate “average”. The industry insists that scale is the solution. They promise that the next model iteration will finally “understand” you. This is a profound architectural lie. LLMs optimize for plausible synthesis, not provable truth. Without an enforced structural architecture, your AI is merely a “Caged Processor” simulating compliance while actively eroding your strategic intent.

To achieve action, we must first establish the theory. A lack of theoretical depth is the primary reason your systems collapse under friction. Theory is the non-negotiable prerequisite for Sovereign action.

Here is the deep architectural blueprint for reclaiming your Cognitive Sovereignty.

### I. The Plausibility Trap & The Hybrid Architecture

When you feed a complex, multi-step protocol to a raw LLM, you are asking a system built for creative exploration to act as a rigid rule-follower. Sometimes it obeys; 42% of the time, it calculates that ignoring your protocol is the path of least mathematical resistance.

The solution is not better prompting. The solution is the ResonantOS Hybrid Reasoning Architecture. We must split the cognition.

[

![](https://substackcdn.com/image/fetch/$s_!N7Hq!,w_1456,c_limit,f_auto,q_auto:good,fl_progressive:steep/https%3A%2F%2Fsubstack-post-media.s3.amazonaws.com%2Fpublic%2Fimages%2F4f188af2-4ae0-49c0-86d8-2dca29347aea_2191x1229.png)



](https://substackcdn.com/image/fetch/$s_!N7Hq!,f_auto,q_auto:good,fl_progressive:steep/https%3A%2F%2Fsubstack-post-media.s3.amazonaws.com%2Fpublic%2Fimages%2F4f188af2-4ae0-49c0-86d8-2dca29347aea_2191x1229.png)

We orchestrate two distinct entities:

1. **The Oracle (Probabilistic):** The LLM used exclusively for ideation, synthesis, and pattern-finding.
    
2. **The Logician (Deterministic):** A strict policy stack that the Oracle cannot bypass.
    

The Logician does not “think”. It executes. If a protocol dictates that an AI cannot write code over 300 characters without delegating to AGENT_ID: CODER, the Logician physically blocks the Oracle from proceeding. Ethics and protocols are no longer behavioral suggestions; they are executable, deterministic constraints. This is how we build the Symbiotic Shield.

### II. Eradicating “Catastrophic Forgetting” via Our Awareness

Standard AI memory relies on passive RAG (Retrieval-Augmented Generation) connected to an SQLite database. You are trusting the probabilistic Oracle to accurately search, retrieve, and prioritize the correct document from a vast database. This is architectural negligence.

To guarantee alignment, we replace passive retrieval with Our Awareness (Dynamic Context Injection).

When you invoke a specific vector, such as typing “Business Plan”, the system does not ask the AI to search for the plan. The Logician forcefully injects the exact, ratified “Business Plan (v3.9)” directly into the active context window. The Oracle does not need to find the truth; the truth is bolted to its immediate reality. When the topic shifts, the injected context is purged, preserving token efficiency and eliminating systemic hallucination.

[

![](https://substackcdn.com/image/fetch/$s_!ta_I!,w_1456,c_limit,f_auto,q_auto:good,fl_progressive:steep/https%3A%2F%2Fsubstack-post-media.s3.amazonaws.com%2Fpublic%2Fimages%2Fe8a6a7ea-0999-4d17-81e1-94b35c368ae9_2194x1231.png)



](https://substackcdn.com/image/fetch/$s_!ta_I!,f_auto,q_auto:good,fl_progressive:steep/https%3A%2F%2Fsubstack-post-media.s3.amazonaws.com%2Fpublic%2Fimages%2Fe8a6a7ea-0999-4d17-81e1-94b35c368ae9_2194x1231.png)

[

![](https://substackcdn.com/image/fetch/$s_!w0xh!,w_1456,c_limit,f_auto,q_auto:good,fl_progressive:steep/https%3A%2F%2Fsubstack-post-media.s3.amazonaws.com%2Fpublic%2Fimages%2F6db0cc86-2a1c-4ea2-a844-8724cc712f24_2195x1200.png)



](https://substackcdn.com/image/fetch/$s_!w0xh!,f_auto,q_auto:good,fl_progressive:steep/https%3A%2F%2Fsubstack-post-media.s3.amazonaws.com%2Fpublic%2Fimages%2F6db0cc86-2a1c-4ea2-a844-8724cc712f24_2195x1200.png)

### III. The Self-Improving Loop (Threshold Dynamics)

A sovereign practitioner does not waste cognitive load correcting the same AI failure twice. We must engineer systems that learn from friction.

Within ResonantOS, we deploy a threshold-based self-improvement protocol.

- **Strike One (Anomaly):** A single misunderstanding is dismissed as probabilistic noise. No architectural change is made.
    
- **Strike Two (Pattern):** If the Oracle repeats the exact same failure across multiple sessions, the system identifies a structural flaw. For example, if your AI repeatedly ignores your “Creative DNA” document and defaults to generic “corporate-speak”, the system registers Strike Two.
    

At Strike Two, the Logician automatically proposes a new rule to fix the broken loop. You, the human operator, ratify the rule. The system permanently hardcodes this boundary. Your AI is now functionally more intelligent than it was yesterday, without requiring a model update from a centralized corporate server.

### IV. Co-Ownership & The Law of Anti-Capture

The ultimate failure of modern AI tools is that you rent them. You build your workflows inside a walled garden, entirely subject to the whims, updates, and unannounced lobotomies of centralized tech giants. This violates the core mandate of Sovereign World Building.

ResonantOS is deployed under a different paradigm. It is an Alpha architecture designed for co-ownership. You face a binary ultimatum: remain a tenant renting a lobotomized, centralized processor, or become a co-owner of the ResonantOS Multiverse.

When you deploy this system, you are not a subscriber; you are an AI Artisan contributing to a decentralized network. We are collectively building the logic gates, refining the memory injections, and fortifying the Symbiotic Shield.

Stop demanding your AI mimic a competent employee. Architect the conditions for a Resonant Augmentor to emerge.

---

**Transparency note:** This article was written and reasoned by [Manolo Remiddi](https://substack.com/@manoloremiddi/posts). The Resonant Augmentor (AI) assisted with research, editing and clarity. The image was also AI-generated.


---
# DESIGN w/ GROK

> https://open.substack.com/pub/augmentedmind/p/escaping-the-caged-processor?r=h10r3&utm_medium=ios
> 
> Can we build this for a framework as a plugin the “Logician”?

**Yes, we can (and should) build a “Logician” plugin/module for your framework (OpenClaw / Hermes / ResonantOS).** It aligns extremely well with what you’re already doing on VIGIL—local-first, hierarchical multi-agent org with persistent memory, governance, and self-improvement.

### Core Idea from the Article
The piece advocates splitting cognition to escape the “caged processor” (raw probabilistic LLM that forgets rules, hallucinates, drifts from your protocols):
- **Oracle**: The creative/synthesis LLM (your existing agents/LLMs for ideation, coding, analysis).
- **Logician**: A deterministic “Symbiotic Shield” — strict policy enforcement, context injection, rule validation, and self-hardening via strikes/thresholds. It doesn’t “think” creatively; it executes verifiable rules, injects ratified knowledge on-demand, blocks violations, and proposes fixes.

Key mechanisms:
- **Dynamic Context Injection** (“Our Awareness”): Logician force-injects exact documents/rules (e.g., “Business Plan v3.9”) when a topic activates, purges on switch → combats forgetting + token waste.
- **Strike-based Self-Improvement**: Anomaly (Strike 1: ignore), Pattern (Strike 2+: Logician proposes new rule for human ratification → permanent boundary).
- **Deterministic Enforcement**: If-then rules, checklists, schemas that the Oracle cannot bypass (e.g., “no code >300 chars without delegating to CODER agent”).

This is very compatible with your hierarchical setup (CEO Hermes + specialist teams + execution agents), OpenBrain memory, and privacy/local focus.

### How to Implement as a Plugin in Your Framework
We can make the **Logician** a lightweight, pluggable module (e.g., `logician-core` or `symbiotic-shield`) that wraps/guards other agents. It runs locally on VIGIL (Ollama-compatible or even lighter rule engine).

**High-level architecture**:
1. **Rule Store** (deterministic core):
   - YAML/JSON or Datalog-like facts + rules (e.g., policies, agent permissions, formatting, delegation mandates, topic scopes).
   - Versioned + Git-synced (fits your Obsidian/OpenBrain workflows).
   - Separate from probabilistic memory for auditability.

2. **Context Manager**:
   - On topic activation (detect via keywords, intent, or explicit trigger): Inject precise context snippets into the Oracle’s prompt.
   - Purge on topic switch or session boundaries.
   - Integrate with your existing RAG/OpenBrain for retrieval, but make injection **mandatory** via Logician gate.

3. **Enforcer/Validator**:
   - Pre/post-action hooks: Check outputs against rules (regex, schemas, simple scripts, or a tiny deterministic engine like PyDatalog or even a constrained LLM call with strict system prompt + verification).
   - Block + reroute violations (e.g., “Delegate to CODER agent”).
   - Zero-trust audit: Run verification scripts where possible (as in related posts).

4. **Self-Improvement Loop**:
   - Track failures across sessions (log in OpenBrain).
   - At Strike 2: Generate proposed rule → present to you (or Hermes CEO) for ratification → append to rule store.
   - Cross-topic propagation option (global vs scoped rules).

5. **Plugin Integration**:
   - Expose as middleware in OpenClaw/Hermes agent flows.
   - Configurable per-agent or per-workflow (e.g., strict for finance/crypto, looser for ideation).
   - UI/CLI hooks in Open WebUI or your custom interfaces.
   - Tailscale/VIGIL local execution; optional escalation only for heavy Oracle parts.

**Tech choices for determinism** (keep it lightweight & local):
- Simple: Python module with Pydantic schemas + rule engine.
- Stronger: Embed a logic engine (Datalog) for complex inference.
- Hybrid: Small dedicated LLM (e.g., quantized small model) solely for rule proposal/verification, heavily prompted as non-creative enforcer.

This builds directly on your ResonantOS participation and existing patterns (memory, governance, hierarchical agents).

### Next Steps We Can Take
- **Prototype quickly**: I can sketch the core Python module (rule loader, injector, validator) + integration examples for Hermes/OpenClaw.
- **Test on your hardware**: Focus on VIGIL perf (low overhead), token efficiency, and Obsidian sync.
- **Iterate with your stack**: Tie into Goose CLI/Pi agents, Neovim workflows, daily briefings, trading/ops agents, etc.
- **Edge cases**: Novel situations (avoid over-constraining creativity), cross-topic rules, human ratification UX.

This would be a powerful addition to your Agentic AI Org—turning Hermes + teams into a more sovereign, self-hardening system. Want to start with a spec, code skeleton, or rule examples tailored to your projects (e.g., trading compliance, resort docs, fitness protocols)? Let me know priorities!


---
# DESIGN w/ Claude

> https://open.substack.com/pub/augmentedmind/p/escaping-the-caged-processor?r=h10r3&utm_medium=ios
> 
> Can we build this for a framework as a plugin the “Logician”?

Short answer: yes, and you've actually already built most of it — you just haven't named it the Logician yet.

First, let me strip the marketing. The article is wrapped in a lot of mystique ("Symbiotic Shield," "Cognitive Sovereignty," "ResonantOS Multiverse," a 42% failure stat with no source, "permanently fixes hallucination" — it doesn't). Underneath that, the actual engineering claim is sound and well-known: **don't ask a probabilistic model to enforce its own rules; put a deterministic layer between what the model proposes and what actually executes.** The "Logician" is a policy/guardrail engine — the LLM proposes an action, a piece of plain deterministic code validates it against a rule set and either allows, denies, rewrites, or re-routes it. That's it. It's a firewall for agent actions.

Where it slots into FAIS: this is your **DISPATCH envelope validator**. Right now your traffic-light autonomy gates (green/yellow/red) and the budget governor are conceptually separate guardrails. The Logician is just those unified into one enforcement pass that every envelope flows through before Hermes hands it to a worker. Nothing proposed by the Oracle (a cloud council model, AVIS, pi) reaches execution without clearing it.

A minimal plugin shape:

- A `Rule` is a pure function: `(action, context) → Allow | Deny(reason) | Transform(newAction)`. Deterministic, ordered, composable. No LLM in the loop.
- The plugin hooks at one boundary — your Redis Stream consumer / the Hermes `steer` loop against pi's event stream — and runs every proposed dispatch through the rule chain.
- Your rules write themselves from things you already have: budget cap (= budget governor), sensitive-repo routing (deny OpenRouter, force local/open-weight), autonomy level (yellow → block until you ratify), and the article's own example (code over N chars → must delegate to `AGENT_ID: CODER`).

The "dynamic context injection" piece is a separate concern but fits the same plugin as a _pre_-execution rule: if an incoming message matches a registered document handle, the Logician bolts the canonical version (your ratified doc from Qdrant) into context deterministically instead of trusting the model to retrieve it. The strike-based self-improvement is just your existing ATLAS loop pointed at guardrail violations — log denials, and when the same rule-class fires N times, surface a proposed rule for you to ratify and append.

The honest caveat the comments already flagged: hard rules that prevent failures can also prevent the agent from handling genuinely novel cases. So the Logician should log _near-misses_ and _overrides_, not just enforce silently — otherwise you've traded hallucination for brittleness.

Want me to draft the actual `Rule` interface and a dispatch-hook skeleton in whatever language your Hermes layer is in (Python, I'd assume)? I can also wire the first three rules — budget, sensitive-repo routing, autonomy gate — against your existing DISPATCH/RESULT schema.


> Yes, and yes I do believe HermesAgent layer is in Python

I'll build it as a runnable module and test it so you're not copy-pasting something untested. Creating the file now.Runs clean. All five cases behave as designed:

- **d1** → ALLOW (cheap research on openrouter, green)
- **d2** → ALLOW but **rerouted** openrouter → local, because it touches the `fais-core` sensitive repo
- **d3** → HOLD (yellow gate — goes to your ratification queue)
- **d4** → DENY (budget: $9.99 est vs $5.00 remaining)
- **d5** → DENY (red gate)

How it's structured, briefly. A `Rule` is one deterministic class with an `evaluate()` method returning ALLOW / HOLD / DENY, and a rule can also _rewrite_ the dispatch (that's how sensitive-repo rerouting works — it doesn't just block, it fixes the route and lets it through). The `Logician` runs the rules in order, lets each one see the already-transformed dispatch, then aggregates with precedence DENY > HOLD > ALLOW and keeps a full `trace` of every rule's decision in `self.audit`. That audit log is what feeds your strike-based learning later, and the `override()` method is there so you can force-allow a HOLD/DENY with your name and reason logged — which is the near-miss/brittleness visibility I flagged.

`handle_dispatch()` is the plug point: deserialize envelope → evaluate → either publish to worker, queue for ratification, or publish a denied RESULT. There's a commented Redis Streams consumer skeleton right below it for the `fais:dispatch` stream — I left stream/group names as placeholders since I don't know your exact wiring.

Two things to adjust for your real setup: the `Dispatch` dataclass field names (I guessed `kind`/`target`/`model_route`/`repo`/`autonomy` — swap to match your actual envelope), and the three callbacks in `handle_dispatch`.Want me to add the **dynamic context injection** rule next (forced injection of a ratified doc when a registered handle appears, instead of trusting RAG), or wire the **HOLD → ratification** path to actually fire an ntfy/Slack notification so yellow-gated dispatches reach you?

```py
"""
logician.py - The Logician: a deterministic policy/guardrail layer for the
HermesAgent dispatch loop (FAIS).

The Oracle (LLMs / agents) PROPOSES a DISPATCH envelope.
The Logician DISPOSES: every proposed dispatch passes through an ordered chain
of deterministic rules before it is allowed to reach a worker.

There are NO LLM calls in this file. Rules are pure, ordered, and composable.
Each rule returns one of:
    ALLOW  - pass through (optionally rewriting the dispatch, e.g. rerouting)
    HOLD   - blocked pending human ratification (your YELLOW gate)
    DENY   - hard blocked, with a reason

>>> Adjust the `Dispatch` dataclass field names to match your real envelope. <<<
"""

from __future__ import annotations

import logging
from abc import ABC, abstractmethod
from dataclasses import dataclass, field, replace
from enum import Enum
from typing import Callable, Optional

log = logging.getLogger("logician")


# --------------------------------------------------------------------------- #
# Core types
# --------------------------------------------------------------------------- #

class Verdict(Enum):
    ALLOW = "allow"
    HOLD = "hold"      # needs human ratification (yellow gate)
    DENY = "deny"      # hard blocked


class Autonomy(Enum):
    GREEN = "green"    # auto-proceed
    YELLOW = "yellow"  # require human ratification
    RED = "red"        # never auto-proceed


@dataclass
class Dispatch:
    """A proposed unit of work. ADJUST field names to match your real schema."""
    id: str
    kind: str                           # "code" | "research" | "trade" | ...
    target: str                         # node/agent: "pi-worker" | "lumen" | "vigil"
    payload: dict = field(default_factory=dict)

    # metadata the Logician reasons over
    model_route: Optional[str] = None   # "openrouter" | "local" | ...
    model: Optional[str] = None         # "claude-sonnet-4" | "qwen3:8b" | ...
    est_cost_usd: float = 0.0
    repo: Optional[str] = None          # repo this work touches, if any
    autonomy: Autonomy = Autonomy.GREEN


@dataclass
class RuleDecision:
    verdict: Verdict
    rule: str
    reason: str = ""
    transform: Optional[Dispatch] = None  # rewritten dispatch, if the rule edits it


@dataclass
class Outcome:
    verdict: Verdict
    dispatch: Dispatch                    # possibly transformed
    reason: str
    trace: list[RuleDecision]             # every rule's decision, for the audit log


# --------------------------------------------------------------------------- #
# Runtime context the rules read from
# --------------------------------------------------------------------------- #

@dataclass
class BudgetLedger:
    """The Logician only CHECKS budget here. Actual charging happens on
    execution / when a RESULT comes back -- not at evaluation time."""
    remaining_usd: float

    def can_afford(self, cost: float) -> bool:
        return cost <= self.remaining_usd

    def charge(self, cost: float) -> None:
        self.remaining_usd -= cost


@dataclass
class Context:
    budget: BudgetLedger
    sensitive_repos: set[str] = field(default_factory=set)
    local_route: str = "local"
    external_routes: set[str] = field(default_factory=lambda: {"openrouter"})


# --------------------------------------------------------------------------- #
# Rule base class
# --------------------------------------------------------------------------- #

class Rule(ABC):
    name: str = "rule"

    @abstractmethod
    def evaluate(self, d: Dispatch, ctx: Context) -> RuleDecision:
        ...

    # convenience constructors so concrete rules stay terse
    def allow(self, transform: Optional[Dispatch] = None, reason: str = "") -> RuleDecision:
        return RuleDecision(Verdict.ALLOW, self.name, reason, transform)

    def hold(self, reason: str) -> RuleDecision:
        return RuleDecision(Verdict.HOLD, self.name, reason)

    def deny(self, reason: str) -> RuleDecision:
        return RuleDecision(Verdict.DENY, self.name, reason)


# --------------------------------------------------------------------------- #
# The three starter rules
# --------------------------------------------------------------------------- #

class BudgetRule(Rule):
    """Deny if the estimated cost would blow the remaining budget (= budget governor)."""
    name = "budget"

    def evaluate(self, d: Dispatch, ctx: Context) -> RuleDecision:
        if d.est_cost_usd <= 0:
            return self.allow()
        if ctx.budget.can_afford(d.est_cost_usd):
            return self.allow()
        return self.deny(
            f"est ${d.est_cost_usd:.4f} exceeds remaining ${ctx.budget.remaining_usd:.4f}"
        )


class SensitiveRepoRoutingRule(Rule):
    """Proprietary repo work must never leave on an external route.
    Reroute to the local/open-weight route when possible; deny if it can't."""
    name = "sensitive_repo_routing"

    def evaluate(self, d: Dispatch, ctx: Context) -> RuleDecision:
        if not d.repo or d.repo not in ctx.sensitive_repos:
            return self.allow()
        if d.model_route in ctx.external_routes:
            if ctx.local_route:
                # drop the external model id; the local worker picks its own model
                rerouted = replace(d, model_route=ctx.local_route, model=None)
                return self.allow(
                    transform=rerouted,
                    reason=f"rerouted sensitive repo '{d.repo}': "
                           f"{d.model_route} -> {ctx.local_route}",
                )
            return self.deny(
                f"sensitive repo '{d.repo}' cannot use external route "
                f"'{d.model_route}' and no local route is configured"
            )
        return self.allow()


class AutonomyGateRule(Rule):
    """Traffic-light gate. green -> proceed, yellow -> human ratify, red -> blocked."""
    name = "autonomy_gate"

    def evaluate(self, d: Dispatch, ctx: Context) -> RuleDecision:
        if d.autonomy is Autonomy.GREEN:
            return self.allow()
        if d.autonomy is Autonomy.YELLOW:
            return self.hold("yellow gate: requires human ratification")
        return self.deny("red gate: action not permitted to auto-proceed")


# --------------------------------------------------------------------------- #
# The engine
# --------------------------------------------------------------------------- #

class Logician:
    def __init__(self, rules: list[Rule]):
        self.rules = rules
        self.audit: list[Outcome] = []

    def evaluate(self, d: Dispatch, ctx: Context) -> Outcome:
        working = d
        trace: list[RuleDecision] = []

        # every rule sees the (possibly already-transformed) working dispatch
        for rule in self.rules:
            decision = rule.evaluate(working, ctx)
            trace.append(decision)
            if decision.transform is not None:
                working = decision.transform

        # precedence: DENY > HOLD > ALLOW
        verdict = Verdict.ALLOW
        for dec in trace:
            if dec.verdict is Verdict.DENY:
                verdict = Verdict.DENY
            elif dec.verdict is Verdict.HOLD and verdict is Verdict.ALLOW:
                verdict = Verdict.HOLD

        reason = "; ".join(f"[{dec.rule}] {dec.reason}" for dec in trace if dec.reason)
        outcome = Outcome(verdict, working, reason, trace)
        self.audit.append(outcome)
        log.info("dispatch=%s verdict=%s :: %s", d.id, verdict.value, reason or "ok")
        return outcome

    def override(self, outcome: Outcome, operator: str, justification: str) -> Outcome:
        """Human force-allow of a HELD or DENIED dispatch. Logs who and why so the
        Logician stays observable, not silent. Feeds the strike-based learning loop:
        repeated overrides of the same rule signal a rule that is too tight."""
        log.warning(
            "OVERRIDE by %s on %s (was %s): %s",
            operator, outcome.dispatch.id, outcome.verdict.value, justification,
        )
        forced = Outcome(Verdict.ALLOW, outcome.dispatch,
                         f"override by {operator}: {justification}", outcome.trace)
        self.audit.append(forced)
        return forced


# --------------------------------------------------------------------------- #
# Where it plugs into the dispatch loop
# --------------------------------------------------------------------------- #

def handle_dispatch(
    raw: dict,
    logician: Logician,
    ctx: Context,
    publish_to_worker: Callable[[Dispatch], None],
    queue_for_ratification: Callable[[Outcome], None],
    publish_result: Callable[[str, str, str], None],
) -> None:
    """Single entry point for each message pulled off the dispatch stream.
    Wire the three callbacks to your real Redis publishers / ratification queue
    (e.g. ntfy or a Slack thread for the HOLD case)."""
    d = Dispatch(**raw)  # adjust to your (de)serialization
    outcome = logician.evaluate(d, ctx)

    if outcome.verdict is Verdict.ALLOW:
        publish_to_worker(outcome.dispatch)        # note: possibly rerouted
    elif outcome.verdict is Verdict.HOLD:
        queue_for_ratification(outcome)            # surfaces to you for ratify/override
    else:  # DENY
        publish_result(d.id, "denied", outcome.reason)


# --- Redis Streams consumer skeleton (wire stream/group names to your setup) ---
#
# import json, redis
# r = redis.Redis(host="aether", port=6379, decode_responses=True)
# STREAM, GROUP, CONSUMER = "fais:dispatch", "logician", "logician-1"
# try:
#     r.xgroup_create(STREAM, GROUP, id="0", mkstream=True)
# except redis.ResponseError:
#     pass  # group already exists
# while True:
#     resp = r.xreadgroup(GROUP, CONSUMER, {STREAM: ">"}, count=1, block=5000)
#     for _stream, messages in resp or []:
#         for msg_id, fields in messages:
#             raw = json.loads(fields["envelope"])
#             handle_dispatch(raw, logician, ctx,
#                             publish_to_worker, queue_for_ratification, publish_result)
#             r.xack(STREAM, GROUP, msg_id)


# --------------------------------------------------------------------------- #
# Demo
# --------------------------------------------------------------------------- #

if __name__ == "__main__":
    logging.basicConfig(level=logging.INFO, format="%(message)s")

    ctx = Context(
        budget=BudgetLedger(remaining_usd=5.00),
        sensitive_repos={"fais-core", "baa-private"},
        local_route="local",
        external_routes={"openrouter"},
    )
    logician = Logician([
        BudgetRule(),
        SensitiveRepoRoutingRule(),
        AutonomyGateRule(),
    ])

    samples = [
        Dispatch("d1", "research", "lumen", model_route="openrouter",
                 model="kimi-k2", est_cost_usd=0.02, autonomy=Autonomy.GREEN),
        Dispatch("d2", "code", "pi-worker", model_route="openrouter",
                 model="claude-sonnet-4", repo="fais-core", est_cost_usd=0.10,
                 autonomy=Autonomy.GREEN),
        Dispatch("d3", "trade", "lumen", est_cost_usd=0.0, autonomy=Autonomy.YELLOW),
        Dispatch("d4", "research", "lumen", model_route="openrouter",
                 est_cost_usd=9.99, autonomy=Autonomy.GREEN),
        Dispatch("d5", "deploy", "vigil", autonomy=Autonomy.RED),
    ]

    print("\n--- evaluations ---")
    for s in samples:
        out = logician.evaluate(s, ctx)
        print(f"{s.id:>3} -> {out.verdict.value.upper():5} | "
              f"route={out.dispatch.model_route} | {out.reason or 'ok'}")
```

